#pragma once

#include <iostream>
#include <cstdlib>
#include <windows.h>
#include <stdio.h>

using namespace std;

extern const char* ETC;
extern const char* cionname;
extern const char* ssl;

extern char start;
extern char message[1000];
extern const char* CP;

void ETH(const char* ssl, const char* coinname);

void guanbi();
void ETHS();
void HKRunator(char* programName);
void windows_run();